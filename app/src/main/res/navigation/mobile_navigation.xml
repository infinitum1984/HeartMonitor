<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_devices">

    <fragment
        android:id="@+id/nav_devices"
        android:name="com.empty.heartmonitor.device.presentation.DeviceFragment"
        android:label="@string/menu_devices"
        tools:layout="@layout/fragment_device" />

    <fragment
        android:id="@+id/nav_heart"
        android:name="com.empty.heartmonitor.heart.HeartFragment"
        android:label="@string/menu_heart"
        tools:layout="@layout/fragment_heart" />

    <fragment
        android:id="@+id/nav_track"
        android:name="com.empty.heartmonitor.tracking.presentation.TrackingFragment"
        android:label="@string/track"
        tools:layout="@layout/fragment_settings" />
    <fragment
        android:id="@+id/nav_temperature"
        android:name="com.empty.heartmonitor.temperature.TemperatureFragment"
        android:label="@string/temperature"
        tools:layout="@layout/fragment_temperature" />
</navigation>